<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CRUD</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>

    <script
      src="https://kit.fontawesome.com/fab4acbac9.js"
      crossorigin="anonymous"
    ></script>

    <style>
      body {
        font-family: 'Arial', sans-serif;
        background-color: #f7f7f7;
      }

      .container {
        max-width: 800px;
        margin: auto;
        padding: 20px;
      }

      .table {
        background-color: #fff;
        border: 2px solid rgb(62, 10, 140);
      }

      th {
        background-color: #f1f1f1;
      }

      .btn-action {
        display: flex;
        gap: 5px;
      }

      .btn-action a {
        text-decoration: none;
      }

      .btn-action button {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .btn-action button i {
        color: #fff;
      }

      .btn-outline-primary {
        background-color: #007bff;
        border: 1px solid #007bff;
        color: #fff;
      }

      .btn-outline-danger {
        background-color: #dc3545;
        border: 1px solid #dc3545;
        color: #fff;
      }
    </style>
  </head>
  <body>
    
    <section class="container">
      <center>
        <div class="p-1">
          <a href="/form" class="btn btn-primary">Add Data</a>
        </div>
      </center>
      <div class="p-5">
        <table class="table table-hover table-bordered">
          <thead>
            <tr class="bg-light">
              <td>Name</td>
              <td>Email</td>
              <td>Contact</td>
              <td>Address</td>
              <td>Action</td>
            </tr>
          </thead>
          <tbody>
            <% data.forEach(function(user){ %>
            <tr>
              <td><%=user.name%></td>
              <td><%=user.email%></td>
              <td><%=user.contact%></td>
              <td><%=user.address%></td>

              <td class="btn-action">
                <a href="/update/<%=user.id%>" class="btn btn-outline-primary">
                  <i class="fas fa-pencil-alt"></i>
                </a>

                <a href="/delete/<%=user.id%>" class="btn btn-outline-danger">
                  <i class="fas fa-trash"></i>
                </a>
              </td>
            </tr>
            <%})%>
          </tbody>
        </table>
      </div>
    </section>
  </body>
</html>